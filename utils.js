// variable = var isDit;
// Boolean = true or false
// Null has no value
// Undefined is als iets geen waarde heeft
// Number = integers
// String = "woord"

// Array = ['1', '2', '3', '4'];
// Function ditiseenfunction(){}
// Function met parameters = function oppervlakte(lengte, breedte){}


// Query pakken uit html = document.querySelector('#antwoord');
// tekst plakken in query =  antwoordTekst.textContent = 'Eng he';

// Hier log je in de console wat je hebt in getyped in het formulier = console.log(document.querySelector('input').value);
// classlist toevoegen = document.querySelector('form').classList.add("trigger_form_animation");

// wanneer er op submit voert hij function uit = document.querySelector('form').addEventListener('submit',renderForm);

// Je genereert een getal tussen 0 en 6 = var randomGetal = Math.random()*6;

// Hoog of laag afronden = var aantalOgen = Math.ceil(randomGetal); (of math.floor)